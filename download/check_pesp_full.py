# 检查单视角视图的文件完整性
import os


# 遍历所有场景
scene_index_list = []
for scene_index in scene_index_list:

    # 检查场景标注
    scene_annos = 

    # 遍历所有样本
    sample_index_list = os.listdir()
    for sample_index in sample_index_list:

        # 遍历所有朝向
        ori_index_list = os.listdir()
        for ori_index in ori_index_list:
    
            # 样本位姿
            
            # 样本深度观测
            
            # 样本语义观测
            
            # 样本 rgb 观测

